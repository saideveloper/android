<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/actionbarBg"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#00000000" >

    <!-- <ImageView -->
    <!-- android:id="@+id/home" -->
    <!-- android:layout_width="wrap_content" -->
    <!-- android:layout_height="wrap_content" -->
    <!-- android:layout_alignParentLeft="true" -->
    <!-- android:paddingLeft="20dp" -->
    <!-- android:layout_centerVertical="true" -->
    <!-- android:src="@drawable/ic_action_navigation_drawer" /> -->

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:paddingLeft="10dp"
        android:shadowColor="@color/black"
        android:shadowDx="2"
        android:shadowDy="2"
        android:shadowRadius="2"
        android:text="TextView"
        android:textColor="@color/white"
        android:textSize="24sp" />

    <ImageView
        android:id="@+id/search"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@+id/callwaiter"
        android:paddingRight="15dp"
        android:src="@drawable/ic_action_search_circle" />

    <ImageView
        android:id="@+id/callwaiter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@+id/order"
        android:paddingRight="15dp"
        android:src="@drawable/ic_action_call_circle" />

    <ImageView
        android:id="@+id/order"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@+id/quicmenu"
        android:paddingRight="15dp"
        android:src="@drawable/ic_order_circle" />

    <ImageView
        android:id="@+id/quicmenu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:paddingRight="10dp"
        android:src="@drawable/ic_action_navigation_drawer_circle" />

    <!-- <ImageView -->
    <!-- android:id="@+id/settings" -->
    <!-- android:layout_width="wrap_content" -->
    <!-- android:layout_height="wrap_content" -->
    <!-- android:layout_alignParentRight="true" -->
    <!-- android:paddingRight="20dp" -->
    <!-- android:layout_centerVertical="true" -->
    <!-- android:src="@drawable/abs__ic_menu_moreoverflow_normal_holo_dark" /> -->

</RelativeLayout>